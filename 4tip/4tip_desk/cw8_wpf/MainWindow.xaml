<Window x:Class="cw8_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cw8_wpf"
        mc:Ignorable="d"
        Title="Główne okienko" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Margin="20" 
                 FontSize="20" Foreground="DarkBlue"
                 Text="{Binding Path=BoundNumber, Mode=TwoWay}"
                 VerticalAlignment="Center"
                 TextAlignment="Center">
        </TextBox>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Slider Minimum="0" Maximum="100" Margin="10" Width="600"
                    Value="{Binding Path=BoundNumber,Mode=TwoWay}"
                    Padding="20"></Slider>
            <Label x:Name="info"></Label>
        </StackPanel>
        <Button Grid.Row="2" x:Name="Reset" Margin="10"
                Click="Reset_OnClick">Resetuj</Button>
        <Button Grid.Row="3" 
                x:Name="OpenWindow" 
                Margin="10" Click="OpenWindow_OnClick"
                >Nowe okno</Button>
    </Grid>
</Window>
