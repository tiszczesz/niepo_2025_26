<Window x:Class="cw7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cw7"
        mc:Ignorable="d"
        Title="Okno główne" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="253*"/>
            <ColumnDefinition Width="147*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="215*"/>
            <RowDefinition Height="219*"/>
        </Grid.RowDefinitions>
        <Slider 
            x:Name="MySlider"
            HorizontalAlignment="Left" 
            Minimum="10" Maximum="100"
            Value="50"
            VerticalAlignment="Center" Width="200" Grid.Column="0" Margin="150,0,0,0" />
        <Label Grid.Row="1" HorizontalAlignment="Left" 
               VerticalAlignment="Center"
               Content="{Binding ElementName=MySlider, Path=Value}" ContentStringFormat="{}{0:0}"
                 Width="500" FontSize="40" FontWeight="Bold" 
                 HorizontalContentAlignment="Center" Grid.Column="0" />
        <TextBox x:Name="MyTextBox" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" 
                 Width="100" Padding="10"
                 VerticalAlignment="Center"></TextBox>
        <Label Grid.Row="1" Grid.Column="1" Content="{Binding ElementName=MyTextBox, Path=Text}"/>

    </Grid>
</Window>
