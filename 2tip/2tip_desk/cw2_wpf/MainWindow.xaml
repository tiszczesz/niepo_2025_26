<Window x:Class="cw2_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cw2_wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
   <Canvas>
       <Label Canvas.Left="100" Canvas.Top="100"
              Foreground="Black" FontSize="30"
              >To jest okno główne MAIN WINDOW</Label>
       <Button Canvas.Left="150" Canvas.Top="250"
               FontSize="20" Foreground="Blue"
               Background="LightBlue" Padding="10"
               Click="ButtonBase_OnClick"
               >Pokaż OTHER WINDOW</Button>
       <Button Canvas.Left="450" Canvas.Top="250"
               FontSize="20" Foreground="Blue"
               Background="LightBlue" Padding="10"
               Click="ButtonClose"
       >Zamknij Aplikacje</Button>
        <CheckBox x:Name="checkBox" 
                  VerticalAlignment="Center"
                  Canvas.Left="163" 
                  Canvas.Top="361">
            <TextBlock FontSize="20" Margin="0 -6 0 0">Pokaż okno modalnie</TextBlock>
        </CheckBox>
    </Canvas>
</Window>
